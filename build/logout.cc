//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "logout.h"
#include <drogon/utils/OStringStream.h>
#include <drogon/utils/Utilities.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string logout::genText(const DrTemplateData& logout_view_data)
{
	drogon::OStringStream logout_tmp_stream;
	std::string layoutName{""};
	logout_tmp_stream << "<!DOCTYPE html>\n";
	logout_tmp_stream << "<html lang=\"ru\">\n";
	logout_tmp_stream << "<head>\n";
	logout_tmp_stream << "    <meta charset=\"UTF-8\" />\n";
	logout_tmp_stream << "    <title>Выход из аккаунта - Financial Manager</title>\n";
	logout_tmp_stream << "    <link rel=\"stylesheet\" href=\"https://unpkg.com/sakura.css/css/sakura.css\" />\n";
	logout_tmp_stream << "</head>\n";
	logout_tmp_stream << "<body>\n";
	logout_tmp_stream << "    <h1>Выход из аккаунта</h1>\n";
	logout_tmp_stream << "    <p>Выход из аккаунта выполняется...</p>\n";
	logout_tmp_stream << "    <p>Если перенаправление не произошло автоматически, <a href=\"/\">нажмите сюда</a>.</p>\n";
	logout_tmp_stream << "    <script>\n";
	logout_tmp_stream << "        (function () {\n";
	logout_tmp_stream << "            try {\n";
	logout_tmp_stream << "                localStorage.removeItem('authToken');\n";
	logout_tmp_stream << "            } catch (e) {\n";
	logout_tmp_stream << "                // ignore\n";
	logout_tmp_stream << "            }\n";
	logout_tmp_stream << "            setTimeout(function () {\n";
	logout_tmp_stream << "                window.location.href = '/';\n";
	logout_tmp_stream << "            }, 500);\n";
	logout_tmp_stream << "        })();\n";
	logout_tmp_stream << "    </script>\n";
	logout_tmp_stream << "</body>\n";
	logout_tmp_stream << "</html>\n";
logout_tmp_stream<<"\n";
logout_tmp_stream<<"\n";
if(layoutName.empty())
{
std::string ret{std::move(logout_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = logout_view_data;
auto str = std::move(logout_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
