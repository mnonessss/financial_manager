//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "welcome.h"
#include <drogon/utils/OStringStream.h>
#include <drogon/utils/Utilities.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string welcome::genText(const DrTemplateData& welcome_view_data)
{
	drogon::OStringStream welcome_tmp_stream;
	std::string layoutName{""};
	welcome_tmp_stream << "<!DOCTYPE html>\n";
	welcome_tmp_stream << "<html lang=\"ru\">\n";
	welcome_tmp_stream << "<head>\n";
	welcome_tmp_stream << "    <meta charset=\"UTF-8\" />\n";
	welcome_tmp_stream << "    <title>Financial Manager</title>\n";
	welcome_tmp_stream << "    <link rel=\"stylesheet\" href=\"https://unpkg.com/sakura.css/css/sakura.css\" />\n";
	welcome_tmp_stream << "</head>\n";
	welcome_tmp_stream << "<body>\n";
	welcome_tmp_stream << "    <h1>Financial Manager</h1>\n";
	welcome_tmp_stream << "    <p>Добро пожаловать! Для продолжения войдите в аккаунт или зарегистрируйтесь.</p>\n";
	welcome_tmp_stream << "    <nav>\n";
	welcome_tmp_stream << "        <ul>\n";
	welcome_tmp_stream << "            <li><a href=\"/auth/register\">Регистрация</a></li>\n";
	welcome_tmp_stream << "            <li><a href=\"/auth/login\">Вход</a></li>\n";
	welcome_tmp_stream << "        </ul>\n";
	welcome_tmp_stream << "    </nav>\n";
	welcome_tmp_stream << "</body>\n";
	welcome_tmp_stream << "</html>\n";
welcome_tmp_stream<<"\n";
welcome_tmp_stream<<"\n";
if(layoutName.empty())
{
std::string ret{std::move(welcome_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = welcome_view_data;
auto str = std::move(welcome_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
